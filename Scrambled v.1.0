#App "Scrambled"


q = int(input("Please enter the number of characters you would like your scrambled password to be: "))
if q == 8:
    #imported from "Scrambled v.1.0(pass8)" file
    import pass8
elif q == 12:
    #imported from "Scrambled v.1.0(pass12)" file
    import pass12
elif q == 16:
    #imported from "Scrambled v.1.0(pass16)" file
    import pass16
elif q == 20:
    #imported from "Scrambled v.1.0(pass20)" file
    import pass20
elif q != 8 or 12 or 16 or 20:
    print("Not a valid entry.")
else:
    "Thank you for using Scrambled."
